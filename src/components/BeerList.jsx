
 import React from 'react';

class BeerList extends React.Component {
  // A component that displays beers in a list format

  constructor(props) {
    super(props);
  }

  render() {
    return (
      <p> Beer list component is alive! </p>
    );
  }
}

export default BeerList;
